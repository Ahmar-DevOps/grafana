import { FC } from 'react';
import { IconType } from '../Icon/types';
interface Props {
    isOpen: boolean;
    title: string;
    body: string;
    confirmText: string;
    icon?: IconType;
    onConfirm(): void;
    onDismiss(): void;
}
export declare const ConfirmModal: FC<Props>;
export {};
