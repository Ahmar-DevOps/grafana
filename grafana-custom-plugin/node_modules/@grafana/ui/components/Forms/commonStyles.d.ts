import { GrafanaTheme } from '@grafana/data';
import { ButtonSize } from '../Button/types';
export declare const getFocusCss: (theme: GrafanaTheme) => string;
export declare const getFocusStyle: (theme: GrafanaTheme) => string;
export declare const sharedInputStyle: (theme: GrafanaTheme, invalid?: boolean) => string;
export declare const inputSizes: () => {
    sm: string;
    md: string;
    lg: string;
    auto: string;
};
export declare const getPropertiesForButtonSize: (theme: GrafanaTheme, size: ButtonSize) => {
    padding: string;
    fontSize: string;
    height: string;
};
